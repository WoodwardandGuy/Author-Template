# E.L. Westbury Site — Readiness Audit

**Date:** 2026-08-10
**Scope:** completeness/visibility, performance, SEO, accessibility, integration readiness.
**Method:** production `next build`, live Sanity content query, WCAG contrast math, static analysis.
**Supersedes:** `AUDIT-REPORT.md` (2026-08-05) — most blockers there are now built (newsletter popup, Flodesk wiring, Book Club gallery + 8 photos, Press page, `/contact` page, signature intro, GA component, CMS bios). This pass audits the current build.

Legend: **PASS** / **FAIL** / **PARTIAL** / **BLOCKER** (must fix before launch)

---

## TL;DR

The site is **structurally complete and fast** — every content page is statically prerendered, content is populated, schema/metadata/alt/images are all in good shape. What stands between it and launch is **configuration, not code**: the canonical URL is still a placeholder, and the email/newsletter/analytics keys are empty, so those features are inert. There are also **two real accessibility contrast issues** introduced by the new palette (wine-as-text) and a handful of polish items.

**Launch blockers remaining:** ② Resend key (contact form) and ④ GA4 id.
**Cleared this pass:** ① real site URL (`elwestbury.com`), ③ Flodesk key, ⑤ wine-as-text contrast — plus all quick code fixes (see below).

---

## Fixes applied — 2026-08-10 (this pass)

| Fix | What changed |
|---|---|
| **Wine-as-text contrast** ✅ | Added `wine.light` (#cf7f8b, AA on soil 6.3:1 / soil-2 5.7:1). Swapped every wine-as-text/link/hover on dark (Events date/time, newsletter success messages, `hover:text-brand` on contact/press/books). Wine stays on buttons/backgrounds. |
| **`/events` h1** ✅ | `Events` section takes a `headingAs` prop; the page now renders it as `<h1>` (was `<h2>` only). |
| **Sitemap `/contact`** ✅ | Added to `app/sitemap.ts`. |
| **Lint tooling** ✅ | `next lint` (removed in Next 16) → flat `eslint.config.mjs` using `eslint-config-next/core-web-vitals`; script is now `eslint .`. ESLint runs clean (also fixed 3 latent issues it caught: two `<a>`→`<Link>` in Header, one effect-setState disable in IntroSignature). |
| **Config (by owner)** ✅ | `NEXT_PUBLIC_SITE_URL=https://elwestbury.com` and `NEWSLETTER_API_KEY` (Flodesk) now set in `.env.local`. |

Verification: `tsc --noEmit` ✅ · `eslint .` ✅ (exit 0) · all pages 200 · `/contact` in sitemap · `/events` has an `<h1>`.

---

## 1. Completeness & visibility — PASS (2 empty content types)

| Area | Status | Note |
|---|---|---|
| All routes build & render | **PASS** | 22 routes compile; content pages prerendered. No 500s; all key routes return 200. |
| Author info (name, tagline, **short+long bio**, portrait, logo) | **PASS** | Fully populated in Sanity. |
| Social links (IG, TikTok, FB, FB group, Goodreads, BookBub) | **PASS** | All six set; footer renders them, `sameAs` in Person schema. |
| Books (3) — covers + real descriptions | **PASS** | All 3 have covers and non-placeholder jacket copy. |
| Hero copy (headline/sub/CTA) | **PASS** | Populated; background image intentionally unused (flat soil per design). |
| Featured release / Brand statement / FAQ (4) / Site copy | **PASS** | All present. |
| Book Club gallery | **PASS** | 8 photos, all approved & visible. |
| Events | **PASS** | 1 upcoming; auto-expires via GROQ. |
| **Press** | **PARTIAL** | Page, schema, nav, sitemap all exist — but **0 press items**, so `/press` shows its empty state. Add items or accept the empty state at launch. |
| **Praise** (home testimonials) | **PARTIAL** | **0 items** → the home Praise section hides gracefully. Add blurbs or leave hidden. |
| Blog | **N/A** | 0 posts; not in nav or sitemap. Template extra, harmless. |
| Images | **PASS** | All via `next/image` (no raw `<img>`), no empty `alt`s. |
| Headings | **PASS** ✅ | Every page has exactly one `<h1>`. `/events` fixed 2026-08-10 (renders its heading as `<h1>` via `headingAs`). |

---

## 2. Performance — PASS (strong architecture; measure LCP before launch)

| Item | Status | Note |
|---|---|---|
| Rendering strategy | **PASS** | All content pages are **Static (○)** or **SSG (●)**; only `/api/*` and `/studio` are dynamic. Excellent TTFB/LCP ceiling. |
| Fonts | **PASS** | Fraunces + Karla via `next/font` (self-hosted, `display: swap`, no external request, no CLS). |
| Images | **PASS** | `next/image` + Sanity CDN with explicit sizes; hero has no image (pure CSS) so LCP is text. |
| Build | **PASS** | Compiles in ~18s; static gen ~0.8s for 22 pages. |
| Exact bundle size (First-Load JS) | **NOT CAPTURED** | Next 16 + Turbopack build omitted the per-route size column. **Recommend a Lighthouse run** (or `next start` + measure) for hard LCP/CLS/TBT before launch. |
| Dead dependencies | **PARTIAL** | `@supabase/supabase-js` and `styled-components` have **0 usages** anywhere — safe to remove. `recharts`, `embla-carousel-react`, `cmdk`, `vaul`, `input-otp`, `react-day-picker` are only pulled in by unused shadcn `ui/` primitives (tree-shaken from routes, so install-weight only). Optional cleanup. |

---

## 3. SEO & author optimization — PARTIAL (one blocker)

| Item | Status | Note |
|---|---|---|
| **Canonical / OG / sitemap base URL** | **RESOLVED** ✅ | `NEXT_PUBLIC_SITE_URL` set to `https://elwestbury.com` (2026-08-10). The `lib/site.ts` fallback is still example.com — harmless as long as the env var is set in **Vercel prod** too (confirm). |
| Per-page metadata (title/description/canonical/OG/Twitter) | **PASS** | Present on all pages via the Metadata API; templated title from root. |
| Structured data (JSON-LD) | **PASS** | Person + WebSite site-wide, Book on book detail pages (with retailer `offers`), FAQPage on home. |
| Sitemap | **PASS** ✅ | Home, books (+ each title), about, events, press, book-club, and **`/contact`** (added 2026-08-10). |
| robots.txt | **PASS** | Allows all, disallows `/studio` + `/api`, links sitemap. |
| Favicon / manifest / touch icons | **PASS** | Full RealFaviconGenerator set in `/public`. |
| Indexing status | **NOTE** | The **coming-soon gate is active in production** (`COMING_SOON` unset ⇒ gated; all routes 503→/coming-soon, which is `noindex`). Intentional pre-launch — nothing is indexed until you set `COMING_SOON=false` in Vercel. |

---

## 4. Accessibility — PARTIAL (contrast)

Contrast ratios computed against the new palette (WCAG AA: 4.5:1 normal text, 3:1 large/UI):

| Pair | Ratio | Verdict |
|---|---|---|
| bone on soil (body) | 15.03:1 | **PASS** |
| bone-dim on soil (secondary) | 10.05:1 | **PASS** |
| bone on wine (button label) | 6.36:1 | **PASS** |
| bone / bone-dim on soil-2 (cards) | 13.7 / 9.2:1 | **PASS** |
| **wine on soil (text/links)** | **2.36:1** | **FAIL** |
| moss on soil (eyebrows, small) | 3.70:1 | **PASS (large/UI) / under AA for small text** |
| line on soil (hairline) | 1.43:1 | Decorative — acceptable but very subtle |

- ✅ **FIXED (2026-08-10).** Added `wine.light` (#cf7f8b) — AA on soil (6.3:1) and soil-2 (5.7:1). All wine-as-text/link/hover on dark now uses it (Events date/time, newsletter success messages, `hover:text-*` on contact/press/books). Wine stays reserved for backgrounds/buttons (bone-on-wine 6.36:1).
- **Minor — moss eyebrows (3.70:1).** Fine as bold/large, but small uppercase labels ideally ≥4.5:1. Consider lightening `moss` slightly.
- Reduced motion honored (intro + former smoke). Inputs have `aria-label`s. Focus rings come from shadcn defaults — verify visible focus on the wine pill/nav.

---

## 5. Integration readiness — 4 config blockers

| Integration | Status | Note |
|---|---|---|
| **Contact form (Resend)** | **BLOCKER** | `RESEND_API_KEY` empty → form returns an error state (graceful, but non-functional). `CONTACT_TO_EMAIL` ✓ set to her Gmail; `CONTACT_FROM_EMAIL=contact@elwestbury.com` must be a **Resend-verified domain**. |
| **Newsletter (Flodesk)** | **RESOLVED** ✅ | `NEWSLETTER_API_KEY` set (2026-08-10). `NEWSLETTER_LIST_ID` still empty (optional segment). Confirm the key is also in **Vercel prod**; restart dev to pick it up locally. |
| **Analytics (GA4)** | **BLOCKER** | `NEXT_PUBLIC_GA_MEASUREMENT_ID` empty → no pageview tracking. Component is env-gated and ready. |
| **On-publish revalidation** | **PARTIAL** | `/api/revalidate` route exists and maps every doc type. `SANITY_REVALIDATE_SECRET` empty + no Sanity webhook configured → production edits won't auto-refresh (SanityLive covers preview only). Set the secret + add the webhook. |
| Sanity read token (draft mode / visual editing) | **MINOR** | `SANITY_API_READ_TOKEN` empty → visual editing/draft preview off. Public content reads fine via CDN. |

---

## 6. Tooling — 1 broken gate

- ✅ **FIXED (2026-08-10).** Added flat `eslint.config.mjs` (spreads `eslint-config-next/core-web-vitals`), removed the legacy `.eslintrc.json`, and set the script to `eslint .`. `eslint .` now runs clean (exit 0) — it also caught and fixed 3 latent issues (two `<a>`→`<Link>` in Header, one intentional effect-setState disable in IntroSignature). Typecheck (`tsc --noEmit`) passes.

---

## Prioritized punch list

### A. Launch blockers (config — owner sets in Vercel/env)
1. ✅ **`NEXT_PUBLIC_SITE_URL`** set to `https://elwestbury.com`.
2. ⬜ **Set `RESEND_API_KEY`** (+ verify the from-domain `contact@elwestbury.com` in Resend) — turns on the contact form.
3. ✅ **`NEWSLETTER_API_KEY`** (Flodesk) set. ⬜ `NEWSLETTER_LIST_ID` still empty — optional (segment); without it, subscribers are added with no segment.
4. ⬜ **Set `NEXT_PUBLIC_GA_MEASUREMENT_ID`** — turns on analytics.
5. ⬜ When ready to go public: **`COMING_SOON=false`** in Vercel.

### B. Launch blockers (code)
6. ✅ **Wine-as-text contrast** fixed (added `wine.light`, AA everywhere on dark).

### C. Should-fix before launch
7. ✅ **`/contact` added to sitemap.**
8. ✅ **`<h1>` added to `/events`.**
9. ⬜ Configure the **Sanity revalidation webhook** (+ `SANITY_REVALIDATE_SECRET`) so her edits go live in seconds.
10. ⬜ Decide on **Press** and **Praise**: add content or accept the empty state / hidden section.

### D. Polish / cleanup (post-launch OK)
11. ⬜ Remove dead deps (`@supabase/supabase-js`, `styled-components`); optionally prune unused shadcn `ui/` primitives.
12. ✅ **Lint fixed** — flat ESLint config; `eslint .` runs clean.
13. ⬜ Lighten **moss** slightly for small-text AA.
14. ⬜ Run a **Lighthouse** pass for hard LCP/CLS/TBT numbers.

---

*Quick code fixes (B6, C7, C8, D12) applied 2026-08-10 — see "Fixes applied" above. Remaining items are owner config (A2/A4/A5), the revalidation webhook (C9), content decisions (C10), and optional polish (D11/D13/D14).*
